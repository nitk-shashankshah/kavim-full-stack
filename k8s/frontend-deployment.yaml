apiVersion: apps/v1
kind: Deployment
metadata:
  name: kavim-frontend
  namespace: kavim
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kavim-frontend
  template:
    metadata:
      labels:
        app: kavim-frontend
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
        - name: kavim-frontend
          image: kavim.azurecr.io/kavim-frontend:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
