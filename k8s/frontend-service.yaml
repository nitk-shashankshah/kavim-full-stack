apiVersion: v1
kind: Service
metadata:
  name: kavim-frontend
  namespace: kavim
spec:
  selector:
    app: kavim-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: https
      port: 443
      targetPort: 443
  type: LoadBalancer
